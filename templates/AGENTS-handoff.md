## Handoff

Large or consequential work ends with a handoff — one page with what
changed, proof it works, an independent review, and the decisions only
the operator can make; use the `handoff` skill from the start. Small
self-contained fixes end with a passing test and a clear commit message —
no handoff. Evidence lives untracked under `.handoff/`; merge is the
operator's act.
