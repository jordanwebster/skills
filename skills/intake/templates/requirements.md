# Purpose: state intent as observable guarantees and surface every assumption for an explicit per-row mark before work begins.

Goal, in the operator's words: <one sentence>

Guarantees — what a user will and will not observe, and how each will be
shown working at the end:

- You will be able to <observable behavior>. Shown by: <screenshot / recording / transcript / test at that boundary>, captured with <tool>.
- This will not <excluded behavior>. Shown by: <evidence>.

Assumptions — largest blast radius first. Small reversible interpretations
may sit as stated defaults, approved by the final all-ok over the recap;
agent-proposed rows always take their own explicit yes; nothing is approved
by silence. On a row: note "depends on row N" where one default hinges on
another mark; list alternatives where the option space is small, so a veto
names its replacement; mark a row "unresolvable by discussion" when only a
throwaway probe or prototype can answer it.

| Question (plain words) | Default (alternatives, if few) | Blast radius if wrong | Provenance | Mark |
| --- | --- | --- | --- | --- |
| <what is unresolved> | <what will be done> | <cost if this is the wrong call> | operator-stated / agent-proposed | [ ] confirm [ ] veto |

Operator acts — everything the operator will be asked to do later, agreed now:

| Act | When it fires | What is asked | Rough cost |
| --- | --- | --- | --- |
| <act or none, e.g. install the capture tool a proof needs> | <trigger> | <the request, plain words> | <minutes and commands> |

Waivers — only if the operator explicitly grants one:

- <waiver, e.g. "run without plan review", or none>

Self-consistency check over the marked set: <ran clean, or the conflict rows it raised>

Final all-ok — the complete record presented once, as a recap: <date, or PENDING>
