# Decisions taken during the unattended build

One line per decision, with rationale. For the operator's later review — recorded,
never pre-approved.

- 2026-08-24 — Keep `scaffold` as the CLI and Python package name because the build plan proposes it and retaining the existing operator vocabulary is the reversible default.
- 2026-08-24 — Require Python 3.11 or newer because it is the build plan's proposed floor and permits a modern stdlib-only implementation.
- 2026-08-24 — Put framework code in the top-level `framework/` directory because the brief forbids edits under `skills/` and a sibling directory keeps code separate from skill instructions.
- 2026-08-24 — Make journal rows self-contained with a hashed `state_after` snapshot because append-before-materialize then gives deterministic crash recovery without a second reconstruction format.
- 2026-08-24 — Identify the plan's one canonical machine block as `<script type="application/json" id="scaffold-plan">` because it is inert HTML, exactly selectable with the standard library, and leaves all surrounding prose non-authoritative.
- 2026-08-24 — Preserve plan order as the deterministic tie-breaker within the ready frontier because it makes dispatch reproducible without inventing a second priority field.
- 2026-08-24 — Limit M1 verification to proving that a typed claim names the product repository's clean immutable `HEAD` because artifact-based result judgment belongs to M2 and must not be smuggled in as a trusted exit-code check.
- 2026-08-24 — Add `.scaffolding/` to the product repository's local Git exclude during `init` because flight state must not enter worker product commits and the exclusion must remain machine-local.
- 2026-08-24 — Restrict task IDs to one 1–120 character ASCII path component because M1 uses them in prompt, result, and claim filenames and plan-controlled traversal must be impossible by construction.
- 2026-08-24 — Retain plan sources at digest-addressed immutable paths and derive the active prompt source from stored state because a rejected re-import may leave unused bytes but must never change an accepted graph's instructions.
- 2026-08-24 — Give every lease a fresh opaque generation ID and require it on claims, releases, and verification because holder names can recur and must not make evidence from an earlier attempt current again.
- 2026-08-24 — Make `init` idempotently repair an absent atomically-written config when its durable store already matches the requested goal because interruption between those writes must not require manual workspace surgery.
- 2026-08-24 — Publish retained plan sources with a same-directory fsynced temporary file, an atomic hard-link create, and a directory fsync because digest paths are write-once and must survive a crash without replacement or partial bytes.
- 2026-08-24 — Parse and retain one byte snapshot of each plan source because the accepted prompt text must always contain the machine contract whose digest and task graph were committed.
- 2026-08-24 — Fsync every retained-plan directory boundary on every import, including identical retries, because an existing entry may be the residue of an interrupted publication whose parent fsync never completed.
- 2026-08-24 — Require each task check to write a versioned JSON result at `SCAFFOLD_RESULT_PATH`, bound to the framework-provided candidate and check digests and containing at least one named observation, because a bare exit code cannot prove what ran.
- 2026-08-24 — Add an explicit `test_changes` plan-task boolean that defaults false because protected test/check edits need machine-readable scope and free-form decision prose is not a safe authorization boundary.
- 2026-08-24 — Capture the product head immediately before dispatch, require the claimed commit to descend from it, and verify from a detached local clone while hashing protected files at both commits because verification must not observe worker-shaped worktree bytes.
- 2026-08-24 — Record a completed red verification as terminal for that task while leaving independent frontier tasks runnable because M2 has no judge yet and silently retrying bad evidence would violate the failure-ends-slice rule.
- 2026-08-24 — Bootstrap a check's first verification timeout from the dispatch budget, then use four times its longest retained successful duration because subsequent timeouts must derive from observed run history while the first observation still needs a finite caller-owned bound.
- 2026-08-24 — Fingerprint each protected Git entry as its tree mode plus blob bytes because mode-only test/check edits are behaviorally meaningful and must pass through the same scope gate as content edits.
- 2026-08-24 — Run each task check in a fresh process group and terminate that whole group on completion or timeout because a verifier verdict must not leave descendant processes acting after checkout cleanup.
- 2026-08-24 — Renew a claimed task's lease before verification for the original lease allowance plus twice the derived check timeout because valid long checks must remain single-owner through artifact application.
- 2026-08-24 — Restore the product branch to its clean pre-dispatch commit before recording or releasing any non-green outcome because later independent work must never inherit an unverified candidate.
- 2026-08-24 — Create and fsync every verification-artifact directory boundary before writing a verdict because the journal must never make a terminal transition durable before its cited evidence path is durable.
- 2026-08-24 — Cover dispatch with its configured timeout plus lease grace, then persist the typed claim and its check-bound lease reservation in one locked journal transition because no reclaimer may enter between candidate filing and framework verification.
- 2026-08-24 — Make an OS-released nonblocking `flock` the one-driver safety boundary and treat heartbeat freshness only as operator-facing liveness because machine sleep may stale time-based signals without releasing ownership.
- 2026-08-24 — Durably record the task, lease generation, product path, and clean pre-dispatch commit immediately before worker mutation because restart can then keep an already-green commit or restore the exact base and release interrupted work as infrastructure without guessing.
- 2026-08-24 — Bind each drain request to the currently locked run and make detached launch wait for a matching heartbeat because stale control files and stillborn children must never be mistaken for a live instruction or successful start.
- 2026-08-24 — Require the locked owner record and heartbeat to agree before process-group stop, and deny the fake worker access to `.scaffolding`, because the kill target and recovery receipts must stay outside the current worker adapter's write authority; real-adapter sandbox enforcement remains M4 work.
- 2026-08-24 — Linearize a drain request with the next task lease under a short-lived boundary `flock` because a request that wins that boundary must prevent new work, while a lease that wins it defines the task the driver is allowed to finish.
- 2026-08-24 — Enforce fake-worker control-state exclusion using both case-folded path components and resolved containment because case-insensitive aliases and symlinks must not bypass the adapter boundary.
- 2026-08-24 — Run each real CLI in a disposable local clone and import only its schema-claimed descendant commit because vendor workspace sandboxes cannot express a portable deny-hole for `.scaffolding` inside an otherwise writable product root.
- 2026-08-24 — Snapshot the delegate roster once per driver slice, accept only its canonical `codex exec` and `claude -p` invocation prefixes, and let only adapter code add model, effort, sandbox, and output flags because operator bindings select minds but cannot override framework safety controls.
- 2026-08-24 — Retain real-worker stdout, stderr, and final messages only after replacing sensitive environment values and known token forms because authentication may be present in the child environment but no credential may survive in flight evidence.
- 2026-08-24 — Remove the disposable clone's source remote before dispatch because copy isolation prevents control-state writes but an origin URL would unnecessarily disclose the active checkout's location; vendor sandboxes' broader read policy remains a recorded residual risk.
- 2026-08-24 — Remove unrelated secret-named variables from the worker environment and reject sensitive values in every changed blob, path, and new commit object before publication because retained-log redaction alone cannot prevent a credential-bearing product commit.
- 2026-08-24 — Terminate the worker's entire process group after every exit and escalate against group liveness rather than leader liveness because a successful or SIGTERM-responsive CLI can otherwise orphan active descendants.
- 2026-08-24 — Invoke Codex with the cross-version top-level `-a never` policy and classify recognizable authentication, network, argument, quota, and pre-dispatch failures as infrastructure because unattended safety must not depend on the newer `--approve-for-me` flag or burn work attempts for transport faults.
- 2026-08-24 — Set both `PWD` and `OLDPWD` to the resolved disposable checkout at the process-launch boundary because vendor workers need no inherited path back to the active product checkout.
- 2026-08-24 — Scan every blob and commit object reachable only from `base..candidate` and enumerate changed paths for every introduced commit because secrets deleted before the claimed final tree and sensitive names backed by reused blobs remain publishable Git history unless both object and path histories are inspected.
- 2026-08-25 — Park a judged task and append any operator escalation to the canonical task state in one journal transition because the question and the work it blocks must survive or disappear together across a crash.
- 2026-08-25 — Use three work failures as the default retry cap because the autopsy's failed run budget was three attempts, while infra failures remain outside that budget and ambiguity is recorded as a diagnostic attempt.
- 2026-08-25 — Treat ambiguity as a framework-owned `defer-to-operator` decision and require model judges only for mechanically detected retry caps because the design forbids retrying ambiguity and no model should be able to weaken that rule.
- 2026-08-25 — Record split, rebrief, and rebind judgments as parked work for later planning-context routing because workers and judges cannot mutate the graph and proposal folding remains a separate M5 chunk.
- 2026-08-25 — Resolve retry-cap judges from the same immutable roster snapshot as the worker slice and run them in a temporary read-only workspace at the roster-owned judge effort because staffing must stay mechanical and a judge needs no product write authority.
- 2026-08-25 — Carry a redacted, 500-character-bounded worker failure detail into judgment because the typed exit class selects accounting while the concrete failure supplies the evidence needed to choose rebrief, rebind, split, park, or impossible.
- 2026-08-25 — Use low, medium, high, and critical as the closed review-severity order, treat the plan-time bar as inclusive, and default an omitted bar to medium because existing plans need a reversible migration while findings at the selected threshold must route consistently.
- 2026-08-25 — Require reviewer claims to carry schema-validated findings while naming the unchanged dispatch-base commit because review completion is typed judgment output and reviewer roles must not mutate the product.
- 2026-08-25 — Derive one implementer remediation task per at-or-above-bar finding and one dependent re-review from plan-owned remediation role, effort, and check fields because reviewers report defects but the framework alone owns graph mutation and staffing.
- 2026-08-25 — Route review findings inside the same journal transition that greens the review, and escalate findings surviving the one re-review without ungreening completed work, because restart must see either the entire successor graph or the entire operator question and never an unrouted review.
- 2026-08-25 — Rewire every planned direct successor of an initial review to the generated re-review in the same transition because remediation must gate all downstream work without rewriting the transitive graph or creating a cycle through its own successor tasks.
- 2026-08-25 — Carry a plan-owned remediation test-changes flag into derived work because the framework, not a reviewer, must authorize protected regression edits needed to fix an at-or-above-bar finding.
- 2026-08-25 — Carry worker proposals inside the typed completion claim and materialize them only with a green verification transition because the existing disposable-clone boundary must not give workers a second path into framework control state.
- 2026-08-25 — Bind each fresh planning pass to a digest-derived ordered batch and the closed in-envelope, beyond-flight, or envelope-breaking dispositions because malformed, partial, duplicated, or stale routing must fail closed before graph mutation.
- 2026-08-25 — Persist beyond-flight routes in the flight's inert local follow-up list because the unattended build has no standing authority to write the operator's external tasks backend; a later authorized handoff can reconcile them without granting execution authority.
- 2026-08-25 — Turn split, rebrief, and rebind judgments into planner-owned replacement proposals that supersede the parked task and rewire its direct successors because judges may classify a failure but must never mutate the dependency graph.
- 2026-08-25 — Give proposal planners no local tools, support that job only through Claude's explicit empty tool list, and fail a Codex planner binding closed because the installed Codex CLI exposes no equivalent no-tool mode and untrusted proposal text must not become a host-read capability.
- 2026-08-25 — Require every in-envelope route to select a plan-owned proposal task template, with `source-task` reserved for judgment replacements, because a runtime planner may shape graph metadata but must never author a shell check that later executes with driver authority.
- 2026-08-25 — Exclude retired, revoked, and superseded tasks from the terminal all-green predicate because lineage removes those nodes from the active goal graph and otherwise a valid judgment replacement can never finish the flight.
- 2026-08-25 — Define each planned demonstration by a replay command, declared product-surface globs, and optional generated artifact paths because the framework needs both a mechanical invalidation boundary and retainable evidence a human can inspect.
- 2026-08-25 — Replay demonstrations in disposable detached checkouts after green task commits and once more at the final product head because first-pass evidence is useful during work but only an exact presented-commit capture may enter done-pending-bless.
- 2026-08-25 — Treat missing or malformed demonstration artifacts as stale, journal their invalidation, and regenerate them instead of adding durability boundaries because demonstration output is re-derivable and crash safety requires correct resume rather than byte preservation.
- 2026-08-25 — Remove secret-named environment variables from demonstration commands, redact recognizable secrets from text transcripts, and reject sensitive generated files because operator-facing evidence must never become a durable credential channel.
- 2026-08-25 — Demote an earlier ready state before every final freshness refresh and compare the clean product head both before and after recording readiness because a failed or concurrent refresh must leave no stale acceptance surface.
- 2026-08-25 — Spool demonstration process output to temporary files and inspect generated-file type and size before bounded reads because capture limits must bound memory and FIFOs or oversized files must not hang an unattended driver.
- 2026-08-25 — Bind explicit acceptance to a SHA-256 subject over the plan, presented head, and exact candidate captures, then graduate all candidates and the acceptance record in one journal transition because an operator must never bless a changed proof surface or a partially persisted regression set.
